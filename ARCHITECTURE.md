# AVR 프레임워크 아키텍처

이 문서는 AVR 을 **제3자가 채택 가능한 프레임워크**로 다루기 위한 구조 문서다.
"우리 제품이 어떻게 만들어졌는가"가 아니라 **"AVR 이 무엇으로 이루어져 있고, 무엇이
프레임워크이고 무엇이 아닌가"** 를 규정한다.

- 적합성 판정 규칙: [`CONFORMANCE.md`](CONFORMANCE.md)
- 변경 절차: [`GOVERNANCE.md`](GOVERNANCE.md)
- 제3자 채택 절차: [`ADOPTION.md`](ADOPTION.md)

---

## 1. 문제 정의 — 지금은 명세이지 프레임워크가 아니다

현재 `framework/` 는 잘 쓰인 내부 명세다. 그러나 프레임워크가 되려면 셋이 더 필요하다.

| 결손 | 증상 |
|---|---|
| **채택 가능성** | `levels` 문구 중 기계 판정이 불가능한 것이 있고, rubric 이 규정하지 않은 값(AI 봇 목록, 질문형 판정 어휘 등)을 구현이 임의로 정했다. 두 구현체가 같은 사이트에 다른 점수를 낸다. |
| **적합성 검증** | 어떤 구현이 "AVR 을 구현했다"고 주장할 때 증명할 방법이 없다. |
| **거버넌스** | 누가 어떤 절차로 배점을 바꾸는지 규정이 없다. 실제로 v1.0.0 → v1.2.0 으로 두 번 바뀌었다. |

이 문서는 첫 번째와 세 번째의 구조를 다루고, 두 번째는 `CONFORMANCE.md` 가 다룬다.

---

## 2. 계층

AVR 은 5개 계층으로 이루어진다. **의존은 위에서 아래로만 흐른다.**

```
┌───────────────────────────────────────────────────────────────────────┐
│ ① SPEC — 산문 명세                                                    │
│    framework/spec/*.md                                                │
│    사람이 읽고 판단하는 정본. 왜 이 값인지, 무엇을 금지하는지.        │
│    책임: 정의 · 근거 · 금지 규칙 · 한계 공시                          │
│    의존: 없음 (최상위)                                                │
└───────────────────────────────────────────────────────────────────────┘
                    │ 규정한다
                    ▼
┌───────────────────────────────────────────────────────────────────────┐
│ ② RUBRIC — 기계가 읽는 정본 (SSoT)                                    │
│    framework/rubric/pillars.yaml · channels.yaml                      │
│    책임: 항목 id · 배점 · levels 문구 · blocking · 채널 가중치        │
│    의존: ① 의 규칙을 데이터로 표현. 코드 의존 없음                    │
│    불변식: pillar weight 합 = 100 · channel weight 합 = 1.0 ·         │
│            blocking = 정확히 2개                                       │
└───────────────────────────────────────────────────────────────────────┘
                    │ id 가 일치해야 한다
                    ▼
┌───────────────────────────────────────────────────────────────────────┐
│ ③ SCHEMA — 출력 계약                                                  │
│    framework/schema/report.schema.json                                │
│    책임: 리포트 산출물의 형태. 무엇을 반드시 공시해야 하는지          │
│    의존: ②의 id enum. ①의 공시 의무를 required 로 강제                │
└───────────────────────────────────────────────────────────────────────┘
                    │ 만족해야 한다
                    ▼
┌───────────────────────────────────────────────────────────────────────┐
│ ④ REFERENCE IMPLEMENTATION — 참조 구현                                │
│    backend/app/domain/   순수 함수 (ARS·AVI·통계·rubric 로더)         │
│    backend/app/audit/    Plane B 수집기 (auto 항목 판정)              │
│    backend/app/probe/    Plane A 프로버 (채널별 어댑터)               │
│    책임: ①②③ 을 코드로 옮긴 **하나의** 구현. 정본이 아니다           │
│    의존: ②③ 을 파싱. ① 을 주석으로 인용                              │
└───────────────────────────────────────────────────────────────────────┘
                    │ 사용한다
                    ▼
┌───────────────────────────────────────────────────────────────────────┐
│ ⑤ APPLICATION — 제품                                                  │
│    backend/app/api/ · backend/app/orchestration/ · frontend/ ·        │
│    docs/viz/ · 데이터베이스 · 과금 · 리포트 렌더링                    │
│    책임: 상품화. 프레임워크의 일부가 아니다                           │
│    의존: ④                                                            │
└───────────────────────────────────────────────────────────────────────┘

        ┌──────────────────────────────────────────────────┐
        │ ⑥ CONFORMANCE — 적합성 스위트 (①②③ 과 나란히)   │
        │    framework/conformance/cases/*.json            │
        │    책임: ①에서 유도한 입력·기대출력 쌍            │
        │    의존: ①②③ **만**. ④ 를 절대 참조하지 않는다   │
        └──────────────────────────────────────────────────┘
                 │ 검증한다 (④ 를 향해 아래로)
                 ▼  임의의 구현
```

### 2.1 의존 방향 규칙 (강제)

1. **아래 계층이 위 계층을 바꾸게 하지 않는다.** 구현이 어려워서 rubric 문구를 고치는 것은
   허용되지만, 그 결정은 ①②의 변경 절차(`GOVERNANCE.md`)를 거쳐야 하고 구현 편의는
   근거로 인정되지 않는다.
2. **④ 는 ②의 숫자를 복제하지 않는다.** 배점·가중치는 파싱해서 쓴다.
   복제하는 순간 문서와 코드가 갈라지고, 진단 제품에서 배점 drift 는 리포트 신뢰도 붕괴다.
3. **⑥ 은 ④ 를 참조해 만들지 않는다.** 케이스를 구현에 맞춰 만들면 구현의 버그가 표준이 된다.
   기대값은 ① 의 수식에서 독립적으로 유도한다.
4. **⑤ 는 채점 규칙을 갖지 않는다.** 라우트가 점수를 계산하면 규칙 변경이 API 를 흔든다.

### 2.2 현재 상태의 위반 사항

| 위반 | 위치 | 성격 |
|---|---|---|
| `MIN_PILLAR_COVERAGE = 0.5`, `AVI_THRESHOLD = 25.0`, `ARS_THRESHOLD = 60.0`, `α/β/γ` 가 코드 상수 | `backend/app/domain/scoring.py` | ① 에는 있으나 ② 에는 없다. 그래서 코드가 복제할 수밖에 없다 → §6.2 참조 |
| 자동 판정 임계·어휘가 코드에만 존재 | `backend/app/audit/collectors/*` | ② 가 규정하지 않은 운영 정의 → §6 |
| `report.schema.json` 을 만족하는 문서를 아무도 생성하지 않는다 | ⑤ 에 직렬화기 부재 | ③ 이 계약으로 작동하지 않는 상태 |

---

## 3. 무엇이 프레임워크이고 무엇이 제품인가

이 경계가 흐려지면 "AVR 을 채택한다"가 "이 회사 제품을 산다"와 구분되지 않는다.

| | 프레임워크 | 참조 구현 | 제품 |
|---|---|---|---|
| 5 Pillar 45항목 정의 | ✅ | | |
| 배점 25/25/20/20/10 | ✅ | | |
| 게이팅 규칙과 상한 40 | ✅ | | |
| ARS/AVI 산식, Wilson, z-test | ✅ | | |
| Gap Matrix 임계값 25/60 | ✅ | | |
| 리포트 JSON 스키마 | ✅ | | |
| 골든 케이스 | ✅ | | |
| robots.txt 파서 | | ✅ | |
| HTML 파서·수집기 | | ✅ | |
| 채널별 LLM 프로버 어댑터 | | ✅ | |
| 원가·레이트리밋·재시도 정책 | | ✅ | |
| 웹사이트·대시보드 | | | ✅ |
| 프로빙 스케줄러·큐 | | | ✅ |
| 과금·티어·계정 | | | ✅ |
| 리포트 PDF 렌더링·문안 | | | ✅ |
| 시각화(`docs/viz/`) | | | ✅ |

**핵심**: 웹사이트·대시보드·프로빙 파이프라인은 프레임워크가 아니다.
AVR 을 채택한 제3자는 자기 대시보드, 자기 스케줄러, 자기 파서를 만들 수 있고 만들어야 한다.
그가 우리와 공유하는 것은 **정의·산식·출력 계약·골든 케이스** 넷뿐이다.

### 3.1 "리포트 문안"은 어디에 속하는가

애매한 경계다. 현재 `avr-model.md` §4.1 의 사분면별 처방과
`plane-b-readiness.md` §4.4 의 게이팅 경고 문구는 **명세에 들어 있다.**

판정: **경고·공시 문구는 프레임워크, 영업 문구는 제품.**

- 프레임워크: 게이팅 발동 경고, 판정 불능 경고, 프록시 격차 공시, 유의하지 않은 변화의 표기,
  커버리지 부족 경고. 이것들은 **빠뜨리면 측정이 거짓말이 되는** 항목이므로 강제 대상이다.
- 제품: 사분면 처방의 구체적 실행 항목, 30/60/90 로드맵, 가격·기간 제안.

---

## 4. 확장 지점 (extension points)

제3자가 AVR 을 자기 시장에 맞게 늘리는 방법. 어느 경우든 `GOVERNANCE.md` 의 버전 규칙을 따른다.

### 4.1 채널 추가 (Plane A)

**언제**: 새 답변 표면이 유의한 이용률을 얻었을 때 (예: 카카오, Copilot, Grok).

```
1. channels.yaml 에 항목 추가
2. 전 채널 weight 재정규화 (합 = 1.0). 기존 채널의 몫을 깎아 자리를 만든다
3. weight_rationale 에 근거를 쓴다. "추정"이면 추정이라고 쓴다
4. report.schema.json 의 channel_id enum 갱신
5. 프로버 어댑터 구현 (§4.3)
6. 골든 케이스: 새 가중치로 avi-six-channels-weighted 계열 케이스 재계산
```

**버전 영향**: **MAJOR**. 가중치가 바뀌면 AVI 시계열이 단절된다. 과거 리포트와 나란히 놓을 수 없다.

**금지**: 고객별로 채널을 켜고 끄면서 같은 AVI 라고 부르는 것. 측정된 채널 집합이 다르면
`weight_renormalized = true` 와 `coverage` 를 반드시 공시한다(`plane-a-visibility.md` §2.2).

### 4.2 항목 추가 (Plane B)

**언제**: 새 시장·새 버티컬에서 기존 45항목이 포착하지 못하는 신호가 확인됐을 때.
한국 특수 항목 4개(`P1-10`, `P2-05`, `P3-08`, `P4-09`)가 선례다.

```
1. 어느 Pillar 에 속하는지 결정. Pillar 를 새로 만들지 않는다 (§4.5)
2. pillars.yaml 에 항목 추가. **같은 Pillar 안에서 자리를 만든다** —
   Pillar weight 는 건드리지 않으므로 항목이 늘면 항목당 몫이 자동으로 줄어든다
3. levels 0/1/2 를 기계 판정 가능한 문구로 쓴다 (§5 의 판정 가능성 체크리스트)
4. check: auto | manual 결정. blocking 은 추가하지 않는다 (§4.4)
5. spec/plane-b-readiness.md 의 표 갱신
6. report.schema.json 의 item_id enum 갱신
7. 골든 케이스 최소 3개: level 0/1/2 각각의 대표 입력
```

**버전 영향**: **MINOR**. 총점은 비교 가능하나 해당 Pillar 세부는 비교 불가.

### 4.3 프로바이더 추가 (Plane A 수집 수단)

**채널과 프로바이더는 다르다.** 채널은 *측정 대상 표면*이고, 프로바이더는 *그 표면에
접근하는 수단*이다. ChatGPT 채널을 공식 API 로도, 브라우저 자동화로도 잴 수 있다.

프로바이더가 만족해야 할 계약 (프레임워크가 규정하는 최소 출력):

| 필드 | 의미 | 없으면 |
|---|---|---|
| `status` | ok / not_shown / off_topic / failed | 분모 계산 불가 |
| `text` | 응답 본문 | mention 판정 불가 |
| `citations[]` | 응답이 출처로 제시한 URL 전부 | `cit_share = null` |
| `model_id` | 실제 호출한 모델 식별자 | 프록시 격차 δ 공시 불가 |
| `search_mode` | on / off / always_on | Matched Cohort 비교 불가 |

**강제 규칙**
- `not_shown`(채널 특성)·`off_topic`(질문 설계 결함)·`failed`(우리 장애)를 뭉치지 않는다.
  원인도 처방도 다르다. 뭉치면 측정을 통째로 폐기하게 된다(내부 분석 §10.4, 비공개).
- 인용 URL 은 **응답 본문이 아니라 도구 결과 블록에도** 있다. 본문만 파싱하면 CitShare 가
  구조적으로 0 이 되고 AVI 의 γ=0.2 가 죽는다. 실측에서 실제로 발생한 결함이다.
- 공식 API 가 없는 채널(`api_available: false`)은 δ 산출 대상에서 제외하고,
  대신 수집 방식과 중단 가능성을 `limitations` 에 명시한다.

### 4.4 blocking 항목 추가 — 금지

blocking 은 **정확히 2개**(`P1-01`, `P1-03`)로 고정한다.
3개 이상이면 게이팅이 흔해져 "40점 만점 리포트"가 양산되고 변별력이 사라진다
(`plane-b-readiness.md` §4.2). 이 제약은 확장 지점이 아니라 **불변식**이며,
바꾸려면 MAJOR 버전과 별도 승인이 필요하다.

### 4.5 Pillar 추가 — 권고하지 않음

Pillar 를 늘리면 배점 재배분이 불가피하고, 그 순간 **모든 과거 총점이 비교 불가**가 된다.
새 신호는 기존 Pillar 안의 항목으로 표현한다. 5 Pillar 구조 자체가 부적합하다는 근거가
쌓였다면 그것은 확장이 아니라 **AVR v2** 다.

### 4.6 로케일 확장

한국 특수 항목이 선례를 만들었다. 다른 시장으로 확장할 때:

- 특정 시장 전용 항목은 `description` 에 `[한국 특수]` 처럼 **명시적으로 표시**한다.
- 해당 시장이 아닌 대상을 진단할 때 그 항목은 `unavailable`(사유: 대상 시장 아님)이지
  0점이 아니다. 0점으로 두면 해외 브랜드가 구조적으로 낮은 점수를 받는다.
- 시장별 rubric 을 분기(fork)하지 않는다. 분기하는 순간 점수의 국제 비교가 끝난다.

---

## 5. 기계 판정 불가 항목 처리 원칙

### 5.1 문제

`levels` 문구 중 **기계 판정이 성립하지 않는** 것이 6개 확인됐다.
자동 감사기 구현 레인이 보고한 실제 사례다.

| 항목 | 판정 불가 문구 | 왜 불가능한가 |
|---|---|---|
| `P2-02` L2 | "목표 질문 세트 상위 20개 중 10개 이상 대응" | 질문 세트가 rubric 밖의 외부 입력 |
| `P2-07` L2 | "비교·선택형 콘텐츠마다" | 콘텐츠 유형 분류가 선행돼야 함 |
| `P3-04` L2 | "질문이 실제 목표 질문 세트에서 유래" | 외부 입력 + 유래 판정 |
| `P3-07` L2 | "해당 프로필이 실제 동일 엔티티를 가리킴" | 엔티티 동일성은 사람 판단 |
| `P3-09` L1/L2 | "경고 페이지당 평균 2건" | 외부 도구(리치 결과 테스트) 출력 필요 |
| `P5-02` L1 | "빌드마다 자동 갱신되어 신뢰할 수 없음" | 두 시점 이상의 관측 필요 |

`check: auto` 인데 판정이 불가능하면 구현은 셋 중 하나를 한다.
① 임의 대리 지표를 만든다 ② 조용히 낮은 등급을 준다 ③ `unavailable` 로 뺀다.
**①②는 같은 사이트에 두 점수를 만든다.** 실제로 참조 구현은 항목마다 다르게 대응하고 있다.

### 5.2 세 가지 선택지

| | (A) 문구를 기계 판정 가능하게 고친다 | (B) 외부 입력을 명세의 일부로 승격한다 | (C) manual 로 확정하고 자동 티어에서 제외 |
|---|---|---|---|
| 채택 가능성 | ★★★ 제3자가 코드만으로 재현 | ★★☆ 입력 규약까지 맞춰야 함 | ★★★ 재현 대상이 아님 |
| 원래 의도 보존 | ★☆☆ 대리 지표로 바뀌며 의미가 얕아짐 | ★★★ 그대로 보존 | ★★★ 그대로 보존 |
| 무료 자동 티어 커버리지 | ★★★ 유지 | ★★☆ 입력이 있을 때만 | ★☆☆ 감소 |
| 구현 비용 | 낮음 | 중간 (입력 스키마 + 검증) | 없음 |
| 위험 | **점수의 의미가 조용히 바뀐다.** 문구만 보고 옛 의미로 읽는 사람이 생긴다 | 입력이 없을 때의 처리를 규정하지 않으면 다시 갈린다 | manual 비율이 25% → 더 높아져 셀프서브 제품이 성립하지 않는다 |

### 5.3 권고

**일반 원칙 — 우선순위 (B) → (A) → (C).**

- **(B) 를 먼저 본다.** 필요한 것이 "판단"이 아니라 "입력"이면, 그것은 판정 불가 항목이
  아니라 **명세가 입력을 빠뜨린 항목**이다. 문구를 훼손하지 않고 해결된다.
- **(A) 는 의미가 보존될 때만.** 대리 지표가 원래 의도의 부분집합이면 채택하고,
  다른 것을 재게 되면 채택하지 않는다.
- **(C) 는 최후.** manual 은 무료 티어를 잠식하고, 사람이 채점하면 채점자 간 편차가
  다시 생긴다(그때는 rubric 이 아니라 채점자 교육이 문제가 된다).

**공통 강제 규칙 (지금 당장 적용 가능)**

> `check: auto` 항목이 필요한 입력을 갖지 못했으면, 구현은 **반드시 `unavailable` + 사유**를
> 낸다. 임의 대리 지표로 채우거나 조용히 낮은 등급을 주는 것을 금지한다.
> `unavailable` 은 분모에서 빠지므로(`plane-b-readiness.md` §3.1) 브랜드에 불이익이 없고,
> 무엇을 못 쟀는지가 리포트에 남는다.

**항목별 권고**

| 항목 | 권고 | 내용 |
|---|---|---|
| `P2-02` | **(B)** | 목표 질문 세트를 Plane B 의 정식 입력으로 승격한다. 이미 Plane A 의 정본 입력(`probe_config.question_set_id`)으로 존재하므로 새 개념이 아니다. 세트가 없으면 `unavailable`(사유: 질문 세트 미제공). 질문형 헤딩 비율 40% 조건만으로 2점을 주지 않는다 |
| `P3-04` | **(B)** | 위와 동일. 같은 입력을 공유하므로 한 번의 명세 변경으로 둘이 해결된다 |
| `P2-07` | **(A)** | "비교·선택형 콘텐츠마다"를 구조 신호로 재정의한다. 예: *"`<table>` 을 포함한 모든 페이지에서 `<th>` 헤더 행이 존재하고, 표의 셀 내용이 이미지가 아닌 텍스트이며, 페이지 집합 전체에 `<ol>` 이 1회 이상 사용됨."* 콘텐츠 유형 분류에 LLM 판정기를 도입하는 대안은 **거부한다** — 채점이 비결정론적이 되면 적합성 자체가 성립하지 않는다 |
| `P3-09` | **(A)** | 외부 도구 출력 의존을 끊는다. "경고"를 *명세가 열거한 권장 속성의 누락 건수*로 재정의하고 목록을 rubric 에 싣는다. 외부 도구에 의존하는 항목은 그 도구가 바뀌는 순간 점수가 바뀌고 제3자가 재현할 수 없다. 재정의 전까지는 **(C) 로 임시 강등** |
| `P3-07` | **(A)** | level 2 조건에서 "해당 프로필이 실제 동일 엔티티를 가리킴" 절을 삭제하고, 자동 판정은 *sameAs 4개 이상 + 지식베이스 1 + 기업 DB 1 + 전 링크 200 응답*까지로 한정한다. 엔티티 동일성 확인은 이미 manual 인 `P4-05`(제3자 카테고리 정의 일치도)의 검증 항목으로 이관을 검토한다 |
| `P5-02` | **(A) + 관측 규약** | 1회 크롤로는 판정이 불가능하다는 것이 본질이다. *"동일 URL 의 본문 해시가 불변인데 `dateModified` 가 변경됨"* 을 **두 시점 스냅샷 비교**로 정의하면 기계 판정이 된다. 다만 그러려면 명세에 **관측 창(observation window)** 개념을 도입해야 하고, 이는 "감사는 1회 크롤"이라는 현재 전제를 깬다. 도입 전까지는 level 1 을 판정 대상에서 제외하고 **0/2 만 판정**하며, 1 에 해당할 가능성이 있으면 `unavailable` |

**결정이 필요한 지점**: 위 권고 중 `P2-07`·`P3-09`·`P3-07`·`P5-02` 는 `levels` 문구 변경이므로
MINOR 버전 상승과 해당 항목의 과거 비교 불가 표기를 수반한다(`GOVERNANCE.md` §4).
`P2-02`·`P3-04` 의 (B) 는 문구를 바꾸지 않으므로 버전 상승 없이 가능하다.

---

## 6. 운영 정의 등록부 (operational definition registry)

### 6.1 왜 필요한가

rubric 이 규정하지 않아 **구현이 정해버린** 값들이 있다. 이 값이 다르면 같은 사이트에
다른 점수가 나온다. 즉 이것들은 사실상 채점 규칙인데 명세 밖에 있다.

아래는 참조 구현에서 실제로 확인된 것들이다. **명세로 끌어올릴 후보 목록**이며,
승격 우선순위는 *점수에 미치는 영향 × 구현마다 갈릴 가능성*으로 매겼다.

| 우선 | 항목 | 운영 정의 | 현재 위치 | 승격 대상 |
|---|---|---|---|---|
| **P0** | `P1-02` | AI 봇으로 인정하는 user-agent 목록 (참조 구현은 6종 + 19종 = 25종). 이 목록의 길이가 "4개 이상 선언" 판정을 직접 바꾼다. `Scrapy` 처럼 AI 봇이 아닌 것도 포함되어 있다 | `audit/collectors/robots.py` | `pillars.yaml` P1-02 levels 또는 별도 `rubric/agents.yaml` |
| **P0** | 전역 | user-agent 매칭 규칙 (대소문자 무시 · **완전 일치**, 부분 문자열 아님) | 동상 | `plane-b-readiness.md` §2 |
| **P0** | `P2-08` | "일치율"의 정의. 참조 구현은 **정규화 후 바이트 완전 일치** 빈도이지 유사도가 아니다. 한 글자만 달라도 0 | `audit/collectors/structure.py` | rubric levels 문구 |
| **P0** | `P2-08` | 정의문 패턴: 주제어 토큰이 `^\S{1,20}(은\|는)$` 이고 문장이 `다.`/`입니다.` 로 끝나며, 주제어가 **첫 3토큰 안**에 있을 것 | 동상 | rubric levels 문구 |
| **P0** | `P2-05`·`P2-06` | 문장 분리 규칙(종결부호 + **공백**이 뒤따를 때만 분리 — `다.` 뒤에 공백이 없으면 분리되지 않는다)과 어절 정의(`text.split()`) | `audit/collectors/htmlutil.py` | `plane-b-readiness.md` §6 (한국 특수) |
| **P0** | `P2-06` | "단락"에 `<li>`·`<td>`·`<th>` 를 포함할지 여부. 포함 여부가 분모를 크게 바꾼다 | 동상 | rubric levels 문구 |
| **P1** | `P2-02` | 질문형 판정 어휘 23개 + 접미 6종, **부분 문자열 매칭**. 영어 `how` 가 `show`·`know` 안에서 걸리고 `가요`·`나요` 가 단어 중간에서 걸린다 | `audit/collectors/structure.py` | rubric levels 문구 또는 `rubric/lexicon.ko.yaml` |
| **P1** | `P2-02` | 목표 질문 대응 판정: 정규화 완전 일치 또는 **키워드 겹침 60% 이상** | 동상 | §5.3 의 (B) 승격과 함께 |
| **P1** | `P2-03` | 헤딩 표제어 추출 규칙: 불용어 12개, 조사 17종 최장일치 제거, 2자 미만 토큰 폐기. "직후 2문단"을 **첫 2문장**으로 해석 | 동상 | rubric levels 문구 |
| **P1** | `P3-07` | 지식베이스 호스트 4종·기업 DB 호스트 11종 목록. rubric 은 4개만 예시한다. 매칭이 **부분 문자열**이라 `notlinkedin.com.evil` 도 통과한다 | `audit/collectors/schema_org.py` | `rubric/` 별도 파일 |
| **P1** | `P1-03` | "본문 텍스트" = 가시 텍스트 **문자 수**. CSR 셸 판정에 쓰는 200자·40자 임계와 마운트 id 6종 목록 | `audit/collectors/render.py`, `htmlutil.py` | rubric levels 문구 |
| **P1** | 전역 | **표본 상한**: sitemap URL 20개, 자식 sitemap 3개, sameAs 링크 10개, 외부 링크 25개. 전부 **앞에서 N개**이지 무작위 표본이 아니다. 표본이 다르면 비율이 다르다 | 여러 수집기 | `plane-b-readiness.md` 새 절 |
| **P1** | 전역 | "핵심 랜딩"의 정의. `P2-01`·`P2-07`·`P3-03` 등 여러 항목이 이 개념에 의존하는데 **정의가 어디에도 없다.** 참조 구현은 호출자가 넘긴 URL 목록으로 대체한다 | 정의 부재 | `plane-b-readiness.md` §2 |
| **P1** | `P1-01` | 재시도 정책. rubric level 0 은 "**재시도 후에도** 5xx"를 요구하는데 참조 구현은 1회 요청 후 판정한다. 일시 장애가 게이팅을 발동시킬 수 있다 | `audit/collectors/fetching.py` | `plane-b-readiness.md` §4.6 |
| **P2** | `P5-01`·`P5-02` | 날짜 인식 문법(ISO·한국어)과 "최종 수정일" 라벨 어휘 10종 | `audit/collectors/freshness.py` | rubric levels 문구 |
| **P2** | `P1-04` | llms.txt 링크 = **Markdown 링크만**. 맨 URL 은 세지 않는다 | `audit/collectors/llmstxt.py` | rubric levels 문구 |
| **P2** | 전역 | 타임아웃 15초, 본문 5MB 상한, 리다이렉트 추적 | `audit/collectors/fetching.py` | 참조 구현 권장값으로 둔다 (점수 영향 간접) |
| **P0** | Plane A | 브랜드 언급 판정 규칙: 한글 브랜드는 `브랜드(조사)?` 뒤에 한글이 오지 않을 것, 라틴 브랜드는 영숫자 경계. 조사 25종 목록 | `probe/providers/parsing.py` | `plane-a-visibility.md` §0 |
| **P0** | Plane A | rank 추출 규칙: 번호 리스트면 그 번호, 불릿이면 순서, 리스트가 없으면 경쟁사 등장 위치 순서, 그것도 없으면 `rank = null` | 동상 | `plane-a-visibility.md` §1.3 |
| **P0** | Plane A | 인용 도메인 정규화: 소문자화 + `www.` 제거, 자사 판정은 정확 일치 또는 `.자사도메인` 접미. 리다이렉트 래퍼 4종 언랩 | 동상 | `plane-a-visibility.md` §1.4 |
| **P1** | Plane A | 프롬프트 규약. 참조 구현은 **시스템 프롬프트 없이 질문 문자열만** 보낸다. 시스템 프롬프트를 넣는 구현과는 MR 이 다르다 | `probe/providers/*.py` | `plane-a-visibility.md` §0 |
| **P1** | Plane A | Sentiment SoV 판정기. 명세는 "판정기 모델과 프롬프트를 리포트에 명시"를 요구하지만 참조 구현에는 **판정기가 존재하지 않는다** | 미구현 | 지표를 보고하지 않거나 명세에서 v2 로 미룬다 |

### 6.2 임계 상수의 소재

다음은 ① 에는 있으나 ② 에는 없어서 코드가 복제할 수밖에 없는 값들이다.
**"정본의 숫자를 코드에 복제하지 마라"**(§2 의존 방향)와 충돌한다.

| 상수 | 값 | 명세 위치 | rubric 위치 |
|---|---|---|---|
| `MIN_PILLAR_COVERAGE` | 0.5 | `plane-b-readiness.md` §3.1 | **없음** |
| `AVI_THRESHOLD` | 25.0 | `scoring.md` §1 | **없음** |
| `ARS_THRESHOLD` | 60.0 | `scoring.md` §1 | **없음** |
| `α / β / γ` | 0.5 / 0.3 / 0.2 | `plane-a-visibility.md` §2 | **없음** |
| `z (95%)` | 1.959964 | `plane-a-visibility.md` §3.2 | **없음** |

**권고**: `rubric/scoring.yaml` 을 신설해 이 다섯을 기계가 읽는 정본으로 옮긴다.
그러면 `scoring.md` 는 근거를 서술하고 값은 YAML 이 갖는, 다른 계층과 같은 구조가 된다.
현재는 임계값을 바꾸려면 코드를 고쳐야 하고, 그것은 ①→④ 의존 방향 규칙 위반이다.

---

## 7. 미해결 구조 이슈

숨기지 않고 나열한다. 전부 사용자 판단이 필요한 지점이다.

1. **프레임워크에 단일 버전이 없다.** 현재 버전 축이 넷이다 —
   `pillars.yaml` 1.2.0, `channels.yaml` 1.0.0, `report.schema.json` 1.0.0,
   spec 문서 각각 1.0.0. `framework/README.md` 는 "버전 1.0.0" 이라고 적고 있어
   `pillars.yaml` 1.2.0 과 이미 어긋나 있다. `GOVERNANCE.md` §2 참조.
2. **`report.schema.json` 을 만족하는 문서를 아무도 만들지 않는다.** 출력 계약이 계약으로
   작동하려면 참조 구현에 직렬화기와 스키마 검증 시험이 있어야 한다.
3. **Gap Matrix 의 `borderline` 이 스키마에만 있고 구현에 없다.** `scoring.md` §4.1 이
   요구하는 규칙인데 산출 경로가 없다. 골든 케이스 2건이 이 결손을 고정하고 있다.
4. **사분면 식별자가 두 벌이다.** 스키마는 `Q1`~`Q4`, 참조 구현은
   `leader`/`prepared`/`coasting`/`untapped`. 어느 쪽이 정본인지 명시되어 있지 않다.
5. **`check: auto` 34개 중 참조 구현이 담당하는 것은 27개다.** 나머지 7개
   (P1-08·P1-09·P2-01·P2-04·P2-09·P4-01·P4-08)는 코드에 `UNCOVERED_AUTO_ITEM_IDS` 로
   열거되어 있고, 커버리지 **분모에 남는다** — 우리가 안 만든 것을 분모에서 빼면
   아무것도 구현하지 않은 구현이 커버리지 100% 를 주장하게 된다.

   *(2026-09-11 해소)* 사유가 리포트로 흘러가지 않던 문제는 닫혔다. 미판정 항목은 이제
   전부 `unavailable_kinds` 로 사유를 싣는다. 그 전까지는 파일럿 회차 리포트에만 실리고
   제품 리포트에는 안 실렸다 — 실측으로 45항목 중 25개가 미판정인데 **18개에 사유가
   없었다**(no_collector 7 + awaiting_manual 11). 어휘·의미·분모 규칙·화면 렌더링이 모두
   있었고 **제품 경로만 그 표를 읽지 않았다.**

   어휘는 `not_implemented`/`measurement_failed` 가 아니라 6종이다 (`scoring.md` §8.4):
   `not_applicable`·`design_limit`(분모 제외) / `inconclusive`·`no_collector`·
   `input_missing`·`awaiting_manual`(분모 포함). 구별이 필요한 이유는 고객이 할 수 있는
   일이 서로 다르기 때문이다 — `no_collector` 는 우리 로드맵 결손이고,
   `awaiting_manual` 은 사람이 보면 판정되며, `input_missing` 은 고객이 입력을 주면
   지금 해소된다.
