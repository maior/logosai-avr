# AVR 채택 가이드 — 우리 코드 없이 구현하기

이 문서는 **AVR 을 자기 코드로 구현하려는 제3자**를 위한 것이다.
참조 구현(`backend/`)을 쓰지 않고, 이 디렉토리의 명세만으로 같은 점수를 내는 것이 목표다.

---

## 1. 무엇을 채택하는 것인가

AVR 은 진단 **모델**이다. 제품이 아니다. 채택한다는 것은 다음 넷을 공유한다는 뜻이다.

1. **정의** — 무엇을 재는가 (5 Pillar 45항목 · 6채널 · MR/PosScore/CitShare)
2. **산식** — 어떻게 합치는가 (ARS · AVI · Gap Matrix)
3. **출력 계약** — 무엇을 반드시 공시하는가 (`report.schema.json`)
4. **적합성 케이스** — 같은 입력에 같은 출력이 나오는가 (`conformance/cases/`)

대시보드·스케줄러·파서·과금은 공유하지 않는다. 그건 각자의 제품이다
(`ARCHITECTURE.md` §3).

---

## 2. 읽는 순서

**최소 경로** (Level 1 을 목표로 하는 경우, 반나절)

```
1. spec/avr-model.md        왜 두 평면인가. 이걸 안 읽으면 나머지가 왜 이렇게 생겼는지 모른다
2. spec/plane-b-readiness.md §2·§3·§4   ARS 산식과 게이팅
3. spec/plane-a-visibility.md §1·§2     AVI 산식
4. spec/scoring.md           사분면 판정
5. rubric/pillars.yaml       배점 정본 (읽지 말고 파싱한다)
6. rubric/channels.yaml      채널 가중치 정본
7. schema/report.schema.json 출력 계약
8. CONFORMANCE.md            적합성 수준과 케이스 형식
```

**전체 경로** (Level 3 목표)

위에 더해:

```
9.  spec/plane-a-visibility.md §3·§4·§5   Wilson · z-test · 프록시 격차
10. ARCHITECTURE.md §5·§6                 판정 불가 항목과 운영 정의 등록부
11. GOVERNANCE.md                         버전이 바뀔 때 무슨 일이 일어나는가
```

**함정**: `rubric/pillars.yaml` 부터 읽으면 45항목 체크리스트로 보인다.
그러면 시장의 다른 감사 툴과 같은 물건을 만들게 된다. **`avr-model.md` 를 먼저 읽어라.**
이 모델의 값어치는 항목 목록이 아니라 두 평면의 분리와 Gap Matrix 에 있다.

---

## 3. 최소 구현 체크리스트

### 3.1 Level 1 — 채점기

```
☐ pillars.yaml 을 파싱한다 (배점을 코드에 복제하지 않는다)
☐ 로더가 불변식을 검증하고 위반 시 즉시 실패한다
   ☐ pillar weight 합 = 100
   ☐ channel weight 합 = 1.0
   ☐ blocking 항목 = 정확히 2개
   ☐ 모든 항목의 levels 키 = {"0","1","2"}
☐ ARS
   ☐ normalized_p = raw_p / (2 × 채점된 항목 수)   ← 분모는 전체 항목 수가 아니다
   ☐ 커버리지 50% 미만 Pillar 제외 + 나머지를 100 으로 재정규화
   ☐ 게이팅은 min(raw, cap) 이지 대입이 아니다
   ☐ raw_ars 와 ars 를 둘 다 산출한다
☐ blocking 입력 검증 (넷 다)
   ☐ 사유 없는 누락 / null → 거부
   ☐ 빈 사유 → 거부
   ☐ 채점 + unavailable 중복 → 거부
   ☐ 사유 있는 unavailable → 통과 + 판정불능 플래그
☐ AVI
   ☐ 미측정 채널 제외 후 가중치 재정규화 + coverage 산출
   ☐ CitShare null 인 채널만 α'=0.625 / β'=0.375
   ☐ CitShare null 을 0 으로 채우지 않는다
☐ Gap Matrix
   ☐ AVI ≥ 25 · ARS ≥ 60, 경계 포함
   ☐ 게이팅 적용 후 ars 로 판정한다 (raw_ars 아님)
☐ 산출물이 report.schema.json 을 통과한다
☐ conformance/cases 의 level 1 케이스 전부 통과
```

### 3.2 Level 2 — 자동 항목 판정기

```
☐ levels 문구를 그대로 적용한다 (재량 해석 금지)
☐ 상위 등급 조건을 먼저 평가한다 (spec plane-b §2.1-2)
☐ 모든 채점 항목에 근거를 남긴다. 근거를 못 남기면 점수가 아니라 미판정이다
☐ 판정에 필요한 입력이 없으면 unavailable + 사유. 대리 지표로 채우지 않는다
☐ 네트워크 실패 ≠ 0점. 타임아웃은 unavailable 이다
☐ 4xx 와 5xx 를 구분한다 (RFC 9309 §2.3.1.3 vs §2.3.1.4). 이 둘을 묶으면 오진한다
☐ 담당하지 않는 auto 항목을 목록으로 공시한다
☐ 자기 구현의 운영 정의를 문서로 공시한다 (ARCHITECTURE.md §6 참조)
☐ conformance/cases 의 level 1·2 케이스 전부 통과
```

### 3.3 Level 3 — 프로빙과 통계

```
☐ 채널별 프로버가 status / text / citations / model_id / search_mode 를 낸다
☐ not_shown / off_topic / failed 를 뭉치지 않는다
☐ 인용 URL 을 응답 본문뿐 아니라 도구 결과 블록에서도 수집한다
   (본문만 보면 CitShare 가 구조적으로 0 이 되고 AVI 의 γ 가 죽는다)
☐ 모든 비율에 Wilson score interval. 점추정 단독 보고 금지
☐ 클러스터 보정. n / n_eff / ρ 를 함께 보고한다
☐ 개선 주장에 two-proportion z-test. 유의하지 않으면 "개선"이라고 쓰지 않는다
☐ Matched Cohort 위에서만 비교한다
☐ 프록시 격차 δ 를 공시한다 (API 보유 채널 필수)
☐ 미측정 채널을 0 으로 채우지 않는다
☐ conformance/cases 의 level 1·2·3 케이스 전부 통과
```

### 3.4 흔한 오구현 — 실제로 밟은 지뢰들

| 오구현 | 결과 |
|---|---|
| 미판정을 0점으로 처리 | 감사를 덜 한 사이트가 나쁜 사이트로 보고된다 |
| blocking 항목 미판정을 허용 | 안 재는 것만으로 게이팅을 피한다. 실측 ARS 40 vs 100 |
| robots.txt 404 를 차단으로 처리 | robots.txt 없는 정상 사이트가 총점 40 으로 잘린다. 실측 56점 오차 |
| 게이팅 상한을 `min` 이 아니라 대입으로 | 최악의 사이트가 40점으로 **올라간다** |
| 미측정 채널을 0 으로 | 우리 커버리지 부족이 브랜드 점수 하락으로 둔갑 |
| CitShare null 을 0 으로 | 인용 미제시 채널이 구조적으로 불리해진다 |
| raw n 으로 Wilson | 구간이 실제의 63% 폭 (ρ=0.5, k=4 기준). 없는 정밀도를 보증한다 |
| 판정 불가 문구를 임의 대리 지표로 | 같은 사이트에 두 점수가 생긴다 |

---

## 4. 적합성 자체 검증

### 4.1 케이스 러너를 만든다

```
1. framework/conformance/cases/*.json 을 글롭으로 훑는다  ← 파일명 하드코딩 금지
2. case.operation 별 어댑터를 자기 구현에 연결한다 (CONFORMANCE.md §3.4, 9개)
3. case.expected 의 키만 비교한다 (부분 비교)
4. case.tolerance.absolute 로 수치를 비교한다
5. expected.rejected 이면 거부되는지 확인한다
6. reference_implementation.status == "known_gap" 인 케이스는
   참조 구현의 결손을 표시한 것이다. 당신의 구현이 통과하면 그게 정상이다
```

참조 구현에도 같은 계약의 러너가 있으나 비공개다 — §3 의 어댑터 계약만으로 충분히 만들 수 있다.
어댑터 9개만 갈아끼우면 어떤 구현에도 쓸 수 있게 만들어져 있다.

### 4.2 자기 구현이 정본을 실제로 읽는지 확인한다

가장 흔한 자기기만은 "YAML 을 읽는 척하고 상수를 쓰는 것"이다. 검사법:

```
P1 의 weight 를 25 → 30 으로, P5 를 10 → 5 로 바꾼 사본을 만든다
→ 만점 시나리오는 여전히 100 이어야 하고
→ P1 만 만점인 시나리오는 25 가 아니라 30 이 나와야 한다
   나오지 않으면 배점을 복제한 것이다
```

### 4.3 결과 공시

```
AVR Level 2 conformant · rubric 1.2.0 · channels 1.0.0 · schema 1.0.0
  auto coverage 22/33
  golden cases 28/28 passed
  known gaps 0
```

버전과 커버리지 없는 적합성 주장은 무효다 (`CONFORMANCE.md` §2.4).

---

## 5. 채택 시 반드시 알아야 할 한계

명세가 스스로 밝히는 것들이다. 이걸 모르고 팔면 나중에 방어할 수 없다.

1. **Pillar weight(25/25/20/20/10), α/β/γ(0.5/0.3/0.2), 임계값(25/60) 은 전부 v1 고정값이며
   실증으로 유도된 값이 아니다.** ARS 컷오프 60 만 구조에서 유도된다.
   AVI 컷오프 25 는 **시장 관측 기반 추정치**이고, 그 참조점(GPTO 평균 SMR 35.4%)은
   독립 검증되지 않은 자사 발표 수치다.
2. **네이버의 출처 선정 로직은 비공개다.** 관련 항목(`P1-10`, `P3-08`, `P4-09`)은
   관측 기반 추정이며 인과가 검증되지 않았다. 리포트에서 "네이버가 이 신호를 본다"고
   단정하지 말고 "네이버 AI 답변의 출처로 관측되는 채널"이라고 서술한다.
3. **API 없는 채널 2개**(네이버 AI 브리핑, Google AI Overviews)는 스크래핑에 의존한다.
   약관 리스크·파서 파손·시계열 단절 가능성을 계약서와 리포트에 명시해야 한다.
4. **"AI 언급 → 매출"의 인과를 이 프레임워크는 검증하지 않는다.** AVI 는 성과 측정 지표이지
   결과 보장이 아니다.
5. **`levels` 문구 6개가 기계 판정 불가 상태다** (`ARCHITECTURE.md` §5.1).
   해당 항목에서 당신의 구현과 우리 구현이 다른 점수를 낼 수 있다.
6. **운영 정의가 아직 명세에 없다** (`ARCHITECTURE.md` §6). AI 봇 목록, 질문형 판정 어휘,
   정의문 패턴, 문장 분리 규칙, 표본 상한 등이 그렇다. 이것들을 승격하기 전까지
   Level 2 의 재현성은 완전하지 않다.
7. **의료·금융 등 규제 버티컬**에서는 개선 권고가 광고 규제(예: 의료광고법 사전심의)에
   저촉될 수 있다. 권고를 사실 기반 정보에 한정하고 효과 보장 표현을 제안하지 않는다.
   진단 리포트 자체가 광고물로 해석될 여지에 대한 법률 검토는 채택자의 책임이다.

---

## 6. 열린 질문 — 우리가 정하지 않은 것

**아래는 의도적으로 비워둔다.** 우리가 일방적으로 정할 수 없거나, 정하기 전에
사용자·이해관계자 판단이 필요한 사항이다. 답이 정해지면 이 절을 규칙으로 대체한다.

### 6.1 라이선스

- `framework/` 의 명세·rubric·스키마·골든 케이스에 어떤 라이선스를 적용할 것인가?
  (후보: CC BY 4.0 / CC BY-SA 4.0 / Apache-2.0 / 독점)
- **명세와 참조 구현에 서로 다른 라이선스를 적용할 것인가?**
  (흔한 조합: 명세는 개방, 구현은 독점 — 채택은 늘리되 제품은 지킨다)
- rubric 을 **수정해서** 배포하는 것을 허용할 것인가?
  - 허용하면: 시장 적응이 빨라지지만 "AVR 점수"의 의미가 분화된다
  - 금지하면: 일관성은 지키나 채택 장벽이 생긴다
  - 절충안: 수정 배포는 허용하되 **"AVR" 이름 사용은 적합성 통과를 조건으로** 한다
    (이 절충안을 택하려면 상표·인증 주체가 필요하다 → §6.3)

### 6.2 인용 방침

- 제3자가 AVR 로 낸 점수를 공표할 때 **출처 표기를 요구할 것인가?**
  요구한다면 표기 형식은? (예: "AVR v1.2.0 (aeo.local/framework) 기준")
- 학술 인용을 위한 **정본 인용 형식**이 필요한가? DOI 를 붙일 것인가?
- **"AVI 33%" 같은 수치를 신뢰구간 없이 인용하는 것을 금지**할 수 있는가?
  이 프레임워크의 핵심 차별점이 통계적 정직성인데, 채택자가 점추정만 인용하면
  차별점이 시장에서 지워진다. 라이선스로 강제 가능한 성질의 것인가?

### 6.3 적합성 인증의 주체

- 적합성은 **자기 선언(self-declaration)** 인가, **제3자 검증**인가?
  - 자기 선언: 비용이 없고 채택이 빠르다. 대신 허위 주장을 막을 수 없다
  - 제3자 검증: 신뢰도가 높다. 대신 검증 주체·비용·중립성 문제가 생긴다
- "AVR conformant" 표기를 상표로 보호할 것인가?
- 허위 적합성 주장을 발견했을 때의 조치는?

### 6.4 거버넌스 개방

- rubric 변경 제안을 **외부에서 받을 것인가?** (`GOVERNANCE.md` §4 의 절차는 현재
  내부 승인만 상정한다)
- 받는다면 승인 주체는? 단독 관리자인가, 위원회인가?
- 채택자에게 **MAJOR 변경 사전 통지 의무**를 질 것인가? 통지 채널은?

### 6.5 데이터

- 파일럿·코호트 데이터를 공개할 것인가? 재캘리브레이션의 근거가 되는 분포를 공개하지 않으면
  채택자는 임계값 25/60 을 검증할 수 없고, 그저 믿어야 한다.
- 공개한다면 익명화 수준은? 브랜드명을 지우면 산업별 baseline 을 못 만든다.

---

## 7. 질문할 곳

현재 정해진 채널이 없다 (§6.4 의 열린 질문).
당분간은 이 저장소의 이슈 트래커를 사용한다.
