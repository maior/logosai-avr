# AVR 거버넌스 — 변경 관리

**rubric 변경은 코드 변경이 아니라 측정 단위의 변경이다.**
자를 바꿔놓고 이전 측정치와 나란히 놓으면 그건 거짓말이 된다.

이 문서는 무엇을 바꿀 때 무엇을 해야 하는지, 누가 승인하는지, 과거 리포트와의 비교 가능성을
어떻게 판정하고 표기하는지를 규정한다.

실행 체크리스트는 `.claude/skills/aeo-rubric-bump/SKILL.md` 에 있다.
**이 문서가 규칙이고, 그 스킬이 절차다.** 둘이 어긋나면 이 문서가 우선한다.

---

## 1. 무엇이 버전 관리 대상인가

| 자산 | 파일 | 성격 |
|---|---|---|
| Readiness rubric | `rubric/pillars.yaml` | 기계 정본. `version` 필드 |
| 채널 가중치 | `rubric/channels.yaml` | 기계 정본. `version` 필드 |
| 출력 계약 | `schema/report.schema.json` | `schema_version` const |
| 산문 명세 | `spec/*.md` | 문서 상단 버전 표기 |
| 적합성 케이스 | `conformance/cases/*.json` | 개별 버전 없음. rubric 버전에 종속 |

---

## 2. 버전 축이 넷이다 — 미해결 이슈

현재 `pillars.yaml` 1.2.0, `channels.yaml` 1.0.0, `report.schema.json` 1.0.0,
spec 문서 각각 1.0.0 으로 **네 축이 독립적으로 움직인다.**
`framework/README.md` 는 "버전 1.0.0" 이라고 적고 있어 이미 `pillars.yaml` 1.2.0 과 어긋나 있다.

이것은 결함이다. "AVR 1.2 를 구현했다"는 문장이 무엇을 뜻하는지 정의되어 있지 않다.

**두 가지 선택지** (사용자 결정 필요, `ADOPTION.md` §6 의 열린 질문과 함께 판단)

| | (A) 통합 프레임워크 버전 도입 | (B) 축별 버전 유지 + 조합 표기 |
|---|---|---|
| 방식 | `framework/VERSION` 하나. 어느 축이 바뀌어도 함께 오른다 | 지금 구조 유지. 적합성 주장에 네 버전을 모두 적는다 |
| 장점 | "AVR 1.3" 이 한 뜻으로 읽힌다 | 무엇이 바뀌었는지 정밀하게 드러난다 |
| 단점 | 채널 가중치만 바꿔도 전체 버전이 올라 과잉 신호 | 표기가 길고, 조합이 늘어 호환성 판단이 복잡 |

**잠정 규칙** (결정 전까지): 적합성·리포트 표기에는 반드시 네 버전을 모두 적는다.

```
AVR · rubric 1.2.0 · channels 1.0.0 · schema 1.0.0 · scoring 1.0.0
```

---

## 3. MAJOR / MINOR / PATCH 판정

`aeo-rubric-bump` 스킬의 기준을 정합시킨 것이다.

| 변경 | 버전 | 근거 |
|---|---|---|
| 항목 삭제 | **MAJOR** | 과거 총점과 비교 불가 |
| Pillar 간 배점 이동 | **MAJOR** | 측정 단위 변경 |
| 게이팅 규칙·상한 변경 | **MAJOR** | 점수 체계 변경 |
| blocking 지정 추가·제거 | **MAJOR** | 게이팅 발동 조건 변경 |
| 채널 추가·삭제·가중치 변경 | **MAJOR** | AVI 시계열 단절 |
| Gap Matrix 임계값 변경 | **MAJOR** | 사분면 판정이 뒤집힌다 |
| α/β/γ 변경 | **MAJOR** | AVI 정의 변경 |
| 항목 추가 (같은 Pillar 내) | **MINOR** | 총점 비교 가능, 해당 Pillar 세부는 불가 |
| `levels` 0/1/2 경계 변경 | **MINOR** | 해당 항목 비교 불가 |
| `check: auto ↔ manual` 변경 | **MINOR** | 자동 점수와 커버리지가 달라진다 |
| 판정 불가 문구를 기계 판정 가능하게 수정 | **MINOR** | `levels` 경계 변경과 동급 |
| 운영 정의를 명세로 승격 (값 불변) | **PATCH** | 점수가 바뀌지 않음이 확인된 경우에 한함 |
| `rationale` · 주석 · 오탈자 | **없음** | |

**애매하면 올린다.** 버전 하나 늘어나는 비용보다 조용한 drift 비용이 훨씬 크다.

### 3.1 "점수가 안 바뀐다"는 주장에는 증거가 필요하다

PATCH 를 주장하려면 **골든 케이스 전체가 기대값 변경 없이 통과**해야 한다.
케이스를 고쳐야 통과한다면 그것은 PATCH 가 아니다.

v1.1.0(입력 검증 강화)과 v1.2.0(robots 404 재분류)은 둘 다 `breaking: false` 로 기록되어
있으나 v1.2.0 은 **404 사이트의 점수를 실제로 바꿨다**(ARS 40 → 96.2 급). 산식은 그대로여도
판정이 바뀌었으므로 MINOR 가 맞다. 실제로 MINOR 로 올렸고 `comparability` 에 재측정
필요를 적었다 — 이것이 올바른 처리의 선례다.

---

## 4. 변경 절차

```
① 제안  ──▶ ② 정당화  ──▶ ③ 승인  ──▶ ④ 반영  ──▶ ⑤ 호환성 판정  ──▶ ⑥ 공시
```

### ① 제안

변경 제안에는 다음이 있어야 한다.

- **무엇을**: 항목 id, 배점 before → after, `blocking` 변화, `levels` 경계 변화
- **왜**: 인과 가설 — 이 신호가 AI 답변 노출에 영향을 준다고 믿는 근거
- **근거 데이터**: 관측 표본 수와 분포. 없으면 "**추정**"이라고 명시한다

> 근거 없는 단정을 금지한다. `channels.yaml` 의 `weight_rationale` 이 "(추정)"을 명시하는
> 것이 기준선이다. 근거가 없다는 사실을 적는 것은 약점이 아니라 요건이다.

### ② 정당화 — 다섯 질문

항목을 추가·변경하려면 다음에 답한다. 하나라도 답이 없으면 제안을 반려한다.

1. **인과 가설**: 이 신호가 왜 AI 답변 노출에 영향을 주는가
2. **관찰 방법**: 어떻게 기계 판정하는가. 판정 불가면 `ARCHITECTURE.md` §5 의 세 선택지 중 무엇인가
3. **오탐**: 이 판정이 정상 사이트를 틀리게 벌줄 수 있는 구성은 무엇인가
   (robots.txt 404 오진이 이 질문을 건너뛰어서 생겼다)
4. **중복**: 기존 43항목 중 같은 것을 재고 있는 항목이 있는가
5. **배점 근거**: 왜 이 몫인가. 같은 Pillar 안에서 어디를 깎아 자리를 만드는가

### ③ 승인

| 변경 | 승인 |
|---|---|
| PATCH | 프레임워크 관리자 |
| MINOR | 프레임워크 관리자 + 제품 책임자 |
| MAJOR | 위 + **명시적 별도 승인** |
| **항목 삭제** | MAJOR 절차 + **삭제 전용 승인.** 삭제는 과거 데이터를 통째로 비교 불가로 만든다 |

발행된 리포트가 있는 상태에서의 MAJOR 변경은 **기존 고객에게 통지**가 선행되어야 한다.

### ④ 반영 — 절대 순서

```
rubric YAML  →  합계 검증  →  spec 문서  →  JSON Schema  →  골든 케이스  →  참조 구현 시험
```

합계 검증을 뒤로 미루지 않는다. 배점이 안 맞으면 뒤 작업이 전부 헛일이다.

불변식 (변경 후 반드시 재확인):

- pillar weight 합 = **100** (정수만)
- channel weight 합 = **1.0**
- blocking 항목 = **정확히 2개**, 전부 `check: auto`
- 모든 항목의 `levels` 키 = `{"0", "1", "2"}`
- `pillars.yaml` 항목 id 집합 = `report.schema.json` 의 `item_id` enum
- `channels.yaml` 채널 id 집합 = `report.schema.json` 의 `channel_id` enum

**골든 케이스를 함께 갱신하지 않은 rubric 변경은 미완이다.** 케이스를 안 고치면
스위트가 옛 자를 계속 검증하게 되고, 그 스위트는 아무것도 재고 있지 않다.

### ⑤ 호환성 판정

| 변경 | 총점 비교 | Pillar 비교 | 항목 비교 | 조치 |
|---|---|---|---|---|
| 항목 추가 (Pillar 내 재배분) | ⚠️ 주의 | ❌ | ❌ | 해당 Pillar 세부 비교 불가 표기 |
| 항목 삭제 | ❌ | ❌ | ❌ | 총점 시계열 단절 표기 |
| Pillar 간 배점 이동 | ❌ | ❌ | ❌ | 총점 시계열 단절 표기 |
| `levels` 경계 조정 | ⚠️ 주의 | ⚠️ | ❌ | 해당 항목 비교 불가 표기 |
| 판정 오류 수정 (v1.2.0 형) | ⚠️ 주의 | ⚠️ | ⚠️ | **영향받는 사이트의 재측정 필요**를 명시 |
| blocking 추가·제거 | ❌ | ❌ | ✅ | 게이팅 규칙 변경 표기 |
| 채널 가중치·구성 변경 | AVI ❌ | — | — | AVI 시계열 단절 표기 |
| Gap Matrix 임계값 변경 | 사분면 ❌ | — | — | 소급 재판정 여부 명시 (§5.2) |
| `rationale` 문장 수정 | ✅ | ✅ | ✅ | 없음 |

판정 결과는 `pillars.yaml` 의 `changelog[].comparability` 에 문장으로 기록한다.
**이 문장이 나중에 리포트로 그대로 나간다.** 비워두지 않는다.

### ⑥ 공시

- `changelog` 에 `version` · `date` · `breaking` · `summary` · `comparability` 를 남긴다.
  `comparability` 를 비워두면 변경은 미완이다.
- MINOR 이상이면 리포트 템플릿에 §5.2 의 표준 문구가 실제로 들어갔는지 확인한다.
- 채택자가 있다면 변경 로그를 공개 위치에 게시한다 (공개 방식은 `ADOPTION.md` §6 의 열린 질문).

---

## 5. 과거 리포트와의 비교 가능성 — 표기 의무

### 5.1 강제 규칙

1. **모든 리포트에 `rubric_version` · `channels_version` · `schema_version` 을 싣는다.**
   (`report.schema.json` 이 `required` 로 강제한다) 리포트에 안 적히면 나중에 어느 자로
   쟀는지 알 수 없고, 버전 관리 전체가 무의미해진다.
2. **직전 리포트와 버전이 다르면 비교 문구를 반드시 노출한다.** 문서에만 적고 리포트에
   안 넣으면 고객은 모른 채 두 숫자를 비교하고, 그 비교로 의사결정을 한다.
3. **비교 불가 구간을 가로지르는 개선 주장을 금지한다.**
   Plane A 는 `comparison.series_break = true`, Plane B 는 rubric MAJOR 변경이 그 구간이다.
4. **유의성 없는 변화를 "개선"이라고 쓰지 않는다.** 이것은 버전과 무관한 상시 규칙이다.

### 5.2 표준 문구

**rubric 변경으로 비교 불가**

> ⚠️ **채점 기준 변경 안내**
> 이 리포트는 rubric v2.0.0 으로 채점되었습니다.
> 직전 리포트(v1.2.0, 2026-06-12)와는 배점 체계가 달라 총점을 직접 비교할 수 없습니다.
> 동일 기준 비교가 필요하면 저장된 원자료로 v2.0.0 재채점을 요청하십시오.

**판정 오류 수정으로 재측정 필요**

> ⚠️ **과거 점수 정정 안내**
> rubric v1.2.0 에서 `P1-01` 판정 오류(robots.txt 404 를 차단으로 오인)를 수정했습니다.
> 이전 리포트에서 게이팅이 발동했다면 오탐일 수 있으며, 재측정을 권장합니다.

**임계값 변경**

> ℹ️ **사분면 판정 기준 변경 안내**
> Gap Matrix 임계값이 AVI 25 → (산업별 코호트 중앙값) 으로 전환되었습니다.
> 과거 리포트의 사분면은 **소급 재판정하지 않습니다.** 이전 사분면 라벨은 발행 당시 기준입니다.

### 5.3 소급 재판정의 기본값은 "하지 않음"

고객이 이미 받은 진단 결론이 사후에 바뀌는 것은 신뢰 문제다.
소급 재판정을 하는 경우는 둘뿐이다.

1. **판정 오류의 정정** — 옛 결론이 틀렸음이 확인된 경우 (v1.2.0 형)
2. **고객의 명시적 요청** — 동일 자 비교가 필요해 원자료로 재채점하는 경우

두 경우 모두 재판정 사실과 사유를 리포트에 남긴다. 조용한 재판정은 금지한다.

---

## 6. 폐기(deprecation) 정책

### 6.1 원칙

**즉시 삭제하지 않는다.** 항목·채널·필드를 삭제하면 그것을 참조하던 모든 구현이 깨지고,
그 항목을 담은 과거 리포트가 해석 불가가 된다.

### 6.2 절차

```
① 폐기 예고  →  ② 유예 기간  →  ③ 삭제
   (MINOR)       최소 2 MINOR      (MAJOR)
                 또는 6개월
```

**① 폐기 예고**

rubric 에 `deprecated` 필드를 추가한다. 배점·판정은 **그대로 유지**한다.

```yaml
- id: P3-06
  title: 보조 스키마 (HowTo·BreadcrumbList)
  deprecated:
    since: "1.3.0"
    remove_in: "2.0.0"
    reason: "HowTo 리치 결과가 폐지되어 인용 기여가 관측되지 않는다"
    replacement: "P3-03 으로 흡수"
```

리포트는 해당 항목에 폐기 예정 표시를 노출한다.

**② 유예 기간**

- 최소 **2 MINOR 버전** 또는 **6개월** 중 더 긴 쪽
- 이 기간에 점수는 바뀌지 않는다. 채택자가 이전할 시간을 주는 것이 목적이다
- 골든 케이스는 유지한다

**③ 삭제**

MAJOR 에서만. 항목 삭제 승인 절차(§③)를 따르고 총점 시계열 단절을 표기한다.

### 6.3 스키마 필드의 폐기

`report.schema.json` 의 필드는 **`required` 에서 먼저 빼고**, 다음 MAJOR 에서 삭제한다.
필드를 바로 없애면 그 필드를 읽던 리포트 렌더러가 전부 깨진다.

### 6.4 폐기하지 않는 것

- **`limitations` · `proxy_gap` · `coverage` 등 공시 필드** — 빠뜨리면 측정이 거짓말이 되는
  항목이다. 이것들의 폐기 제안은 자동 반려한다.
- **blocking 2개** — 불변식이다 (`ARCHITECTURE.md` §4.4).

---

## 7. 골든 케이스의 거버넌스

케이스는 rubric 과 같은 무게로 다룬다.

| 행위 | 규칙 |
|---|---|
| 케이스 추가 | 자유. 리뷰만 거친다 |
| 케이스의 `rationale` 보강 | 자유 |
| **기대값 변경** | rubric·spec 변경과 **같은 커밋**에서만. 단독 변경 금지 |
| **케이스 삭제** | MAJOR 승인 필요. 삭제 사유를 기록한다 |
| `known_gap` 추가 | 구현 결손 발견 시. 케이스를 구현에 맞춰 고치는 대신 |
| `known_gap` 해제 | 구현이 고쳐지면 xpass 로 실패하므로 자동으로 강제된다 |

**기대값을 고쳐 시험을 통과시키는 것을 금지한다.** 기대값이 바뀌어야 한다면 그 앞에
명세 변경이 있어야 하고, 명세 변경에는 §4 의 절차가 있어야 한다.
순서가 뒤집히면 구현이 명세를 끌고 다니게 된다.
