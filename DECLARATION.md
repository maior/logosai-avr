# 적합성 자기 선언 — 참조 구현

```
AVR Level 2 conformant · rubric 1.9.0 · channels 1.2.0 · scoring 1.1.0 · schema 1.4.0
  (auto coverage 32/32, known gaps: 0, manual tier: 13 items)
  reliability disclosed: spec/reliability.md §8
  Level 3: NOT claimed — Plane A 프로빙 재현성 미측정
```

선언 주체: **LogosAI / MKII** · 선언일 **2026-09-21** · 구현: AVR 참조 구현 (비공개)

---

## 왜 이 문서가 있는가

`CONFORMANCE.md` §2.4 가 적합성 표기 형식을 규정한다. **그 형식을 정해 놓고 우리 것을
한 번도 내지 않았다.** 남에게 선언하라고 요구하면서 우리가 안 하면, 이 프레임워크는
표준이 아니라 마케팅 문서다.

그리고 **선언은 자랑이 아니라 구속이다.** 아래 숫자는 제3자가 같은 골든 케이스로
대조할 수 있고, 틀리면 우리가 틀린 것이다.

## Level 2 를 선언하고 Level 3 은 선언하지 않는다

| 수준 | 요건 | 상태 |
|---|---|---|
| **Level 1** Scoring | 정본 YAML 파싱, ARS·AVI 산식, blocking 입력 검증, Gap Matrix, 스키마 통과 | ✅ 골든 케이스 `level:1` **38건 전부 통과** |
| **Level 2** Assessment | + `check: auto` 항목 기계 판정, 근거 첨부, 미측정 사유 분류 | ✅ `level:2` **46건 전부 통과** |
| **Level 3** Measurement | + Plane A 프로빙과 통계, **그리고 프로빙 재현성 공시** (`reliability.md` §7) | ❌ **선언하지 않는다** |

Level 3 을 선언하지 않는 이유는 구현이 없어서가 아니다. Plane A 프로빙·통계·클러스터
보정은 구현돼 있고 `level:3` 골든 케이스 10건도 통과한다. **프로빙의 재현성을 아직 재지
않았다.** 같은 질문을 같은 채널에 반복했을 때 결과가 얼마나 흔들리는지 모르는 상태에서
"측정 적합" 을 주장할 수 없다 — 그 요건은 우리가 `reliability.md` 에 직접 써 넣은 것이다.

## 담당 범위

```
check: auto     32개 → 참조 구현이 32개 전부 담당 (미담당 0)
check: manual   13개 → 기계 판정하지 않는다. 심화 진단(사람)에서 해소
골든 케이스      98건 (L1 42 · L2 46 · L3 10) · known_gap 0
```

**`auto` 를 전부 담당한다고 해서 전부 점수가 나온다는 뜻이 아니다.** 예를 들어 `P1-08`
(고아 페이지)은 표본으로 `level 2` 만 증명할 수 있고 나머지는 `design_limit` 이다 —
"유입 0" 은 전수 크롤 없이 증명되지 않는다. 담당한다는 것은 **판정을 시도하고 못 하면
그 사실을 사유와 함께 남긴다**는 뜻이다.

자동 티어의 항목 커버리지는 원리상 `min_item_coverage`(0.7)를 넘지 못한다 — 32/45 =
71.1% 가 이론적 상한이고 실측은 27~31 항목이다. 따라서 **자동 판정만으로 채운 사분면은
언제나 borderline** 이며, 리포트는 인접 사분면의 처방을 함께 낸다.

## 신뢰도 공시

`reliability.md` §3 이 요구하는 세 측정을 실제로 재고 §8 에 실었다. 요약:

```
결정성   : 불일치 0 / 692 항목 (28 대상, 네트워크 0건)
시간 의존 : +90일에도 최대 1.6점 (움직인 항목: P5-03 하나)
재현성   : 항목 일치율 100.00% [80.14%, 100.00%]  n=217 · n_eff=16 · ρ=0.5
           ARS |Δ| 중앙값 0.00 · 최대 0.00  (8곳, 10분 간격)
판별력   : 코호트 27곳 ARS 범위 [29.0, 66.0] · σ 8.1
```

**점추정을 단독으로 내지 않는다.** 100% 의 클러스터 보정 하한은 80.1% 다 — 한 대상 안의
항목은 독립이 아니라 실효 표본이 217이 아니라 16이다.

## 우리가 주장하지 **않는** 것

적합성 선언에서 가장 중요한 절이다. 아래는 증명되지 않았고, 우리는 증명했다고 말하지 않는다.

1. **타당도.** ARS 가 실제 AI 인용을 예측하는지 모른다 (`scoring.md` §2.1). 재현성이
   높다는 것은 "같은 답을 낸다" 이지 "옳은 답을 낸다" 가 아니다
2. **임계값 `ARS ≥ 60` 의 근거.** 44항목 전부 1점이면 정확히 50이라는 구조적 논거는
   있으나, 60이 인용을 가르는 경계라는 실증은 없다
3. **판정자 간 일치도.** `check: manual` 13항목을 두 사람이 같게 채점하는지 재지 않았다.
   심화 진단을 판매하기 전에 재야 한다
4. **프록시 격차 δ.** 월간 캘리브레이션을 **한 번도 돌리지 않았다.** 모든 리포트가
   "δ 미측정" 으로 공시된다 — 그게 정직한 현재 상태다
5. **제3자 재현.** 골든 케이스 94건을 우리 외에 아무도 돌린 적이 없다. 이 선언의 값은
   누군가 대조해 줄 때 비로소 생긴다

## 제3자가 이 선언을 검증하는 방법

```bash
git clone https://github.com/maior/logosai-avr
# conformance/cases/*.json 을 자기 구현에 먹이고 expected 와 대조한다
# 기대값은 spec/*.md 에서 독립 유도했다 (CONFORMANCE.md §3.3) —
# 우리 구현을 돌려 나온 값을 적은 것이 아니다
```

불일치를 찾으면 이슈로 알려 주기 바란다. **우리 구현이 틀린 것으로 밝혀지면 우리가
고치고, 케이스가 틀린 것으로 밝혀지면 케이스를 고친다.** 어느 쪽이든 이 선언을 갱신한다.

## 갱신 규칙

이 문서는 아래가 바뀌면 **같은 커밋에서** 갱신한다.

- rubric·channels·scoring·schema 버전
- 담당 범위 (`auto` 항목 수, 미담당 목록)
- 골든 케이스 수·수준 분포·`known_gap`
- 신뢰도 수치 (재측정할 때마다)
- 선언 수준 (Level 3 을 주장하게 되는 시점)

`backend/tests/test_declaration.py` 가 이 문서의 숫자를 정본과 대조한다. 사람이
기억해서 고치는 방식은 이 저장소에서 이미 두 번 실패했다 (`ARCHITECTURE.md` §7-5,
`README.md` 의 케이스 수).

---

| declaration | 날짜 | 변경 |
|---|---|---|
| **1.0.0** | 2026-09-21 | 최초 선언 — Level 2, rubric 1.9.0, auto 32/32, 골든 94건, 신뢰도 공시 포함 |
