# AEO 표준 실태 — 무엇이 표준이고, 무엇이 아닌가

**조사일 2026-09-10. 모든 1차 출처를 그날 직접 확인했다.**

이 문서는 AVR 채점 규칙이 아니다. **AVR 이 무엇 위에 서 있는지**를 밝히는 문서다.
rubric 의 항목들은 표준을 근거로 하거나, 근거가 없으면 없다고 말해야 한다.
그 구분을 하지 않으면 우리도 "믿음을 파는" 쪽이 된다.

---

## 0. 한 문단 결론

**AEO/GEO 표준은 존재하지 않는다.** ISO·W3C·IETF 어디에도 "답변 엔진 최적화" 명세가 없다.
있는 것은 ① AI 를 위해 만들어지지 않았지만 AEO 가 올라타 있는 진짜 표준 몇 개,
② 거버넌스 없는 제안 하나(`llms.txt`), ③ AI 제공사가 각자 발행하는 크롤러 운영 문서,
④ 벤더가 자기 이름을 붙인 사설 점수뿐이다. 유일하게 진행 중인 진짜 표준화는
**IETF AIPREF WG** 이고 아직 RFC 가 아니다.

따라서 **"표준을 지켰다"는 주장은 현재 성립하지 않는다.** AVR 도 표준이라고 주장하지
않는다. AVR 은 **공개된 측정 모델**이다 — 규범을 선포하는 것이 아니라, 누구든 같은 입력에
같은 점수를 재현할 수 있게 산식과 적합성 케이스를 공개한다는 뜻이다.

---

## 1. Tier 1 — 실제 거버넌스가 있는 표준

AEO 가 올라타 있지만, **AI 답변 엔진을 대상으로 쓰인 것은 하나도 없다.**

| 표준 | 거버넌스 | 버전 / 날짜 | AI 에 대해 말하지 **않는** 것 |
|---|---|---|---|
| **RFC 9309** Robots Exclusion Protocol | IETF Standards Track | 2022-09 | AI·LLM·생성형·답변엔진을 **한 번도 언급하지 않는다.** 크롤 접근 여부만 규정하며 "학습에 쓰지 마라"·"인용해 달라"를 표현할 어휘가 없다 |
| **schema.org** 어휘 | 표준화 기구가 **아니다.** 검색사 steering group (자체 FAQ 가 그렇게 밝힌다) | v30.0 / 2026-03-19 | AI 답변 인용과의 관계를 규정하지 않는다 |
| **JSON-LD 1.1** | W3C Recommendation | 2020-07-16 | 직렬화 형식만 정의. 소비 주체를 가리지 않는다 |
| **Sitemaps** | 표준화 기구 **없음.** 커뮤니티 문서 | 0.9 / 최종 갱신 **2016-11-21** | 10년째 갱신 없음. AI 언급 없음 |
| **IETF AIPREF WG** | IETF WIT Area | `draft-ietf-aipref-vocab`·`-attach`, 목표 2026-08. **RFC 미발행** | AI 용도 선호를 표준화하려는 **유일한** 진짜 시도. 아직 표준이 아니다 |

`P1-01` 이 robots.txt 의 4xx 를 "전체 허용", 5xx 를 "전체 차단"으로 읽는 근거가
RFC 9309 §2.3.1.3·§2.3.1.4 다. 우리가 규범을 발명한 것이 아니다.

> [RFC 9309](https://www.rfc-editor.org/rfc/rfc9309.html) ·
> [schema.org releases](https://schema.org/docs/releases.html) ·
> [schema.org FAQ](https://schema.org/docs/faq.html) ·
> [JSON-LD 1.1](https://www.w3.org/TR/json-ld11/) ·
> [sitemaps.org](https://www.sitemaps.org/protocol.html) ·
> [IETF AIPREF](https://datatracker.ietf.org/wg/aipref/about/)

---

## 2. Tier 2 — 거버넌스 없는 제안: `llms.txt`

2024-09 개인 발의. IETF draft 도 W3C 명세도 아니다. 여기서 두 가지를 **반드시 구분**해야 한다.

| 구분 | 확인 결과 |
|---|---|
| AI 제공사가 자기 `llms.txt` 를 **발행**한다 | ✅ 확인됨 (여러 제공사의 개발자 문서가 자체 색인용으로 제공) |
| AI 제공사가 **남의 사이트의** `llms.txt` 를 읽어 답변·인용에 반영한다 | ❌ **어떤 제공사의 공개 문서에서도 확인하지 못했다** |
| 제공사 측 반박 | Google 공식 문서: *"You don't need to create new machine readable files, AI text files, or markup to appear in these features."* |

"AI 회사가 llms.txt 를 권장한다"는 2차 서술은 대개 **코딩 에이전트가 라이브러리 문서를
읽을 때** 참고하라는 엔지니어링 안내를 옮긴 것이다. **검색·인용 파이프라인이 남의
llms.txt 를 읽는다는 진술이 아니다.** 두 문장을 뭉개면 안 된다.

**AVR 의 입장**: `llms.txt` 는 *에이전트에게 문서를 먹이는 규약*으로는 실사용되지만,
*AI 답변에 인용되기 위한 레버*라는 증거는 없다. 그래서 관련 rubric 항목은
**증거 상태를 함께 공시**하며, 인과가 실증되지 않았다는 사실을 리포트에 적는다
(`plane-b-readiness.md` — 해당 항목의 증거 등급 참조).

---

## 3. Tier 3 — 제공사가 실제로 발행하는 것

여기가 AEO 의 유일한 **1차 근거**다. 표준이 아니라 각사의 운영 문서다.

제품 설계에 직결되는 세 가지:

1. **"AI 봇"은 단일체가 아니다.** 같은 회사 안에서도 *학습용* 크롤러와 *검색·인용용*
   크롤러가 분리돼 있고, 문서가 그 용도를 명시한다. **학습봇을 막으면서 검색봇을
   열어두는 것은 정상 구성이다.** 그래서 `P1-01` 은 봇 6종을 **개별 판정**한다.
   전면 차단/전면 허용의 이분법으로 채점하는 도구는 이 구성을 오진한다.
2. **robots.txt 로 통제되지 않는 경로가 문서상 존재한다.** 일부 제공사는 *사용자 개시*
   요청(사용자가 특정 URL 을 물었을 때의 조회)은 robots.txt 지시를 일반적으로 따르지
   않는다고 스스로 밝힌다. robots.txt 만 보고 "도달 가능"을 단정하면 양방향으로 틀린다.
3. **Google 은 추가 요건이 없다고 공식 진술한다.** *"There are no additional requirements
   to appear in AI Overviews or AI Mode, nor other special optimizations necessary."*
   이 문장은 AEO 업계 전체의 판매 서사와 정면으로 충돌하며, **인용하지 않고 넘어가면
   안 되는 1차 자료**다.

**미확인으로 남긴 것**: 국내 최대 채널의 공식 기준은 1차 자료 취득에 실패했다
(문서 서버가 자동 취득을 차단). 2차 블로그에만 존재하는 서술은 **우리 리포트에서
공식 기준으로 인용하지 않는다.** 관련 항목은 "관측되는 채널"로 서술하고 인과를
단정하지 않는다.

---

## 4. Tier 4 — 벤더 사설 점수

표준이 아니다. 각 벤더가 자기 이름을 붙인 지표다.

2026-09-10 기준 국내외 도구 **7개**를 같은 기준으로 조사했다. 기준은 셋이고, 누구든
공개 페이지만으로 재현할 수 있다.

1. **방법론이 공개돼 있는가** — 지표 정의·질문 수·반복 수·탐지 로직·가중치
2. **표본 수를 공시하는가** — 점수 옆에 `n`
3. **신뢰구간 또는 유의성 검정을 공시하는가** — `CI` 또는 `p`

결과:

| 기준 | 충족 |
|---|---|
| 방법론 전체 공개 | **0 / 7** (부분 공개 2곳) |
| 표본 수 공시 | 2 / 7 |
| **신뢰구간 또는 검정 공시** | **0 / 7** |

이것이 AVR 이 존재하는 자리다. 그리고 이것은 **자랑이 아니라 부채**다 — 우리가
구간을 붙이는 유일한 곳이라면, 우리 구간이 틀렸을 때 바로잡아 줄 사람도 없다는 뜻이다.
그래서 산식과 골든 케이스를 공개한다 (`CONFORMANCE.md`).

---

## 5. 그래서 AVR 은 무엇을 주장하는가

**주장하는 것**

- 두 평면(Visibility / Readiness)을 분리해 재는 것이 진단으로서 더 정확하다
- 모든 비율에 `n`·`n_eff`·`ρ` 와 구간을 붙이지 않으면 그 비율은 검증 불가다
- 미측정과 0점은 다르며, 섞으면 감사를 덜 한 사이트가 나쁜 사이트로 보고된다
- 같은 입력에 같은 점수가 나와야 하고, 그것을 골든 케이스로 증명할 수 있다

**주장하지 않는 것**

- AVR 이 표준이라는 주장. 표준은 우리가 아니라 소비자(AI 제공사)가 정한다
- 특정 항목을 고치면 노출이 오른다는 인과. `avr-model.md` §6 의 회귀가 끝나기 전까지
  우리는 **상관도 인과도 발표하지 않는다**
- 사이트에 우리 마크업을 심으라는 요구. **AI 제공사가 읽지 않는 탑재 규약은 가치가
  정확히 0 이다.** AVR 의 처방은 전부 §1 의 기존 표준 위에서만 나온다

**우리가 틀렸을 수 있는 곳** — Pillar 배점(25/25/20/20/10), α/β/γ(0.5/0.3/0.2),
임계값(AVI 25 · ARS 60)은 v1 고정값이며 실증으로 유도된 값이 아니다. 재캘리브레이션
계획은 `scoring.md` §3.2 에 있다.

---

## 6. 이 문서의 갱신 규칙

- **IETF AIPREF 가 RFC 로 발행되면** §1 을 갱신하고 rubric 반영 여부를 검토한다
- **어떤 제공사든 남의 `llms.txt` 를 읽는다고 공개 진술하면** §2 를 뒤집고 해당 rubric
  항목의 증거 등급을 올린다. **그 진술을 찾는 것은 우리 책임이다** — 없다고 단정한
  문서를 방치하는 것은 있다고 단정하는 것만큼 나쁘다
- **경쟁 도구가 구간을 공시하기 시작하면** §4 의 표를 즉시 갱신한다. 이 표가 낡은 채로
  남아 우리에게 유리하게 읽히는 것이 이 문서의 가장 큰 실패 방식이다

조사 일자를 문서 상단에 반드시 남긴다. 날짜 없는 실태 조사는 실태 조사가 아니다.
